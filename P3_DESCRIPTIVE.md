# P3_DESCRIPTIVE.md — 札幌市データの記述統計・観測過程診断

作成日：2026-07-27。対象：札幌市ヒグマ出没データ（2017-04-15〜2025-11-29、原記録1,568件）。
本フェーズの目的は、モデル化（P4）に進む前に「データが生成される過程の癖」を可視化して把握することであり、
H2・H3・H5の正式検証やモデル選定は行わない。すべての主要図表は**原記録ベース**と**重複候補集約ベース**の
2系列で作成し、両者で見え方が変わる場合はその差自体を結果として記録する。

**H1（人口密度・道路密度との対応）はこの段階では判定しない。** 人口メッシュ・道路密度データを取得していないため、
代替変数での近似も行っていない。「人口効果を確認した」という主張はどこにも書いていない。

図表はすべて `reports/figures/p3_*.png`、再生成スクリプトは `src/viz/common.py` と `src/viz/p3_figures.py`
（`uv run python src/viz/p3_figures.py` で再生成可能）。

---

## 1. データ概要

- 原記録数：1,568件（2017-2025年、9年分）
- 重複候補グループ数（試作ルール v1_naive_1500m_3day_sameward）：1,085グループ
- 座標欠損：1件のみ（0.06%）
- 種別内訳（原記録）：目撃 909、痕跡(フン/足跡等) 410、目撃(未確定種) 141、不明 60、カメラ確認 47、捕獲 1
  → 図表では稀な「捕獲」「不明」を集約して「その他」カテゴリとした（5カテゴリ：目撃／目撃(未確定種)／痕跡／カメラ確認／その他）

---

## 2. 年次件数

**図**: `p3_annual_counts_by_type.png`（種別別・原記録/重複集約の2パネル）、`p3_annual_total_and_duprate.png`（総件数と重複率）

- 年次総件数（原記録）は95件（2020年）〜361件（2025年）まで変動する。2025年は突出して多く、他の年（100〜230件程度）から明確に外れている。
- 重複候補集約ベースでは2025年の値は189件まで縮小し、原記録の約半分になる。これは2025年の急増が「多数の異なる個体の出没」よりも「同一個体（群）に対する複数回の通報」の寄与が大きいことを示唆する（ただし重複判定ルールは試作版であり、断定material根拠ではない）。
- 年次重複率（1 − 重複集約件数/原記録件数）は2019年（約51%）と2025年（約42%）で高く、2024年（約14%）で最も低い。重複率には明確な単調トレンドはなく、年によって大きく変動する。
- 種別構成は目撃が一貫して最大だが、2025年は痕跡（フン・足跡等）とカメラ確認の件数も他年より多く、単に目撃通報が増えただけでなく複数の観測窓すべてで件数が増加している。

---

## 3. 季節性（H4）

**図**: `p3_seasonality_weekly_overlay.png`（週別・年重ね描き・未平滑化）、`p3_seasonality_weekly_by_type.png`（種別別・全年合計）、`p3_weekday_month_heatmap.png`（月×曜日）

### 観察（記述のみ、二峰性を前提としない）

- 年ごとの週別パターンは**年によって形が大きく異なる**。2017・2019年は初夏（週20前後）に単峰的なピークがあり秋の顕著な高まりは見られない。一方2025年は秋（週38〜43、9月中旬〜10月下旬）に極めて大きな単峰的ピークがあり、原記録で週あたり最大36件に達する（重複集約では最大16件）。
- 複数年を重ねると、初夏（週20前後）と秋（週38〜44前後）の両方に山ができる年がある一方、どちらか一方しか目立たない年、明確な山が形成されない年（2020・2024年など、そもそも年間件数が少ない）も混在する。**「毎年必ず春秋の二峰性がある」とは言えず、年によって単峰・多峰・不明瞭のいずれもが観測される。**
- 月×曜日ヒートマップ（全年合計）では、6月・7月・10月に件数が多く、11月〜3月は著しく少ない。これは既知のヒグマの活動期（3月〜12月、冬眠期を除く）と整合的である。
- 種別別に見ると、目撃と痕跡はいずれも夏〜秋にかけて増加する点は共通するが、痕跡（フン・足跡等）は目撃に比べて秋（10月前後）への集中がやや強い傾向が見える（図`p3_seasonality_weekly_by_type.png`）。ただし種別ごとのnが小さい年もあり、この違いが安定した傾向か年次変動かは本記述統計だけでは判別できない。

### H4暫定判定

**判定：部分的支持／年変動が大きく「二峰性」を前提とした一律の記述は不支持。**

- 支持される点：季節性そのもの（夏〜秋に集中し、冬に少ない）は明確。
- 不支持点：「毎年、春と秋に明確な二峰がある」というパターンは、少なくとも未平滑化の生データからは一律には確認できない。年によって単峰・複数の山・不明瞭のいずれもあり、ピークの時期・高さの年変動が非常に大きい（本サーベイのSURVEY.mdでも指摘した通り、この年変動自体が記述的に強い図となる）。
- 原記録と重複集約で「山の位置」自体（どの週が多いか）はおおむね一致するが、「山の高さ」は重複集約で大きく縮小する（特に2025年秋）。**季節パターンの形は両ベースで概ね頑健だが、ピークの大きさの解釈（急増の実態がヒグマの増加か通報の重複か）は原記録だけでは判断できない。**

---

## 4. 曜日周期（H6）

**図**: `p3_weekday_counts.png`（全期間合計）、`p3_weekday_by_year_heatmap.png`（年×曜日）、`p3_weekday_by_type.png`（種別別）、`p3_weekday_month_heatmap.png`（月×曜日）、`p3_weekday_holiday.png`（平日 vs 土日祝）

**重要な留保：日付フィールドの意味は未確定（P2_GATE.md参照）。以下は「曜日差が存在するかどうか」のみを報告し、それが野外活動（人間の休日の外出行動）由来か、行政の受付・入力処理由来かは、本記述統計からは判別できない。断定しない。**

### 観察

- 全期間合計（原記録）の曜日別件数：月225、火235、水221、木240、金223、土211、日213。最大（木240）と最小（土211）の比は約1.14倍。**明確だが大きくはない曜日差が存在する。**
- 土日（平均212）は平日（月〜金平均228.8）よりわずかに少ない（図`p3_weekday_holiday.png`でも、平日と土日・祝日の1日あたり平均件数に差が見られる）。ただし差は小さく、年や種別によって符号が一定するかは年×曜日ヒートマップ（`p3_weekday_by_year_heatmap.png`）を見る限り一様ではない（年によって最も多い曜日が異なる）。
- 種別別（`p3_weekday_by_type.png`）では、種別ごとに曜日パターンが異なる可能性はあるが、痕跡・カメラ確認・その他はnが少なく、曜日差を安定的に読み取るには心もとない。
- 原記録ベースと重複集約ベースで、曜日差の相対的な形（どの曜日が多いか）はおおむね類似しており、重複除去によって曜日パターンの結論が反転するようには見えない。

### H6暫定判定

**判定：曜日差の存在は支持されるが、解釈（原因）は判定不能。**

- 支持される点：曜日ごとの件数に統計的検定なしでも視認できる程度の差（最大約14%）が存在する。
- 判定不能な点：この差が（a）ヒグマの生態とは無関係な人間側の野外活動パターン（週末に人が森林に入る頻度が変わる）由来なのか、（b）自治体の受付・入力業務が平日に偏る（行政処理日）由来なのか、（c）日付フィールドの意味そのものが未確定であるため、そもそも「曜日」が何を意味する曜日なのか（発生曜日か処理曜日か）が確定していない、という3つの理由から、本記述統計だけでは判定できない。
- **P4以降でH6を識別戦略として使う場合は、まず日付フィールドの意味を確定させることが前提条件になる。** 現状のままでは「曜日差がある」という事実以上のことは言えない。

---

## 5. 時刻分布

**図**: `p3_timeofday_distribution.png`（種別別・時刻記録がある通報のみ）、`p3_timeofday_by_month_heatmap.png`（月×時刻）、`p3_timeofday_missing_rate.png`（年別欠損率）

- 時刻欠損（時刻が記録されていない通報）は全体で109/1,568件（約7.0%）。ただし年によって大きく異なり、**2020年は27.4%と際立って高い**（他の年はおおむね3〜13%）。2021年も約13%とやや高い。2022年以降は3〜5%程度に戻っている。この2020年の欠損率急増は、後述のデータ品質診断（第7節）でも「記録方式変更を疑わせる断層」の候補として扱う。
- 「目撃」の時刻分布は、朝7〜8時台と夕方17〜18時台に山があり、正午前後にやや谷がある形（原記録ベース：朝ピーク約47件@7時、夕方ピーク約70件@17時）で、単純な「行政の受付時間帯（9-17時にフラットに多い）」とも、単純な「ヒグマの薄明薄暮性に沿った目撃時刻」とも言い切れない、両方の特徴が混ざったような分布である。
- 「痕跡（フン・足跡等）」の時刻分布は、9〜11時台に鋭いピークがあり（原記録で最大52件@10時）、目撃よりも業務時間帯への集中がより明瞭である。これは、痕跡確認が職員・専門家による現地確認作業に伴って記録される（＝発見時刻ではなく確認作業の実施時刻を反映しやすい）可能性と整合的だが、本記述統計はこの解釈を検証するものではなく、あくまでパターンの記述にとどめる。
- 原記録ベースと重複集約ベースで時刻分布の形はおおむね類似しており、重複除去によって時刻分布の全体的な印象が大きく変わることはない。

---

## 6. 空間記述（1kmメッシュ、詳細座標は不掲載）

**図**: `p3_spatial_mesh_by_type.png`（種別別、全年合計）、`p3_spatial_mesh_yearly_raw_record.png` / `p3_spatial_mesh_yearly_dedup_group.png`（年別、3×3小分割図）

- 出没は札幌市内の特定の森林縁辺エリア（複数の帯状クラスタ）に集中しており、市内全域に一様に分布しているわけではない。年によってホットスポットの位置がやや移動している（例：2017年は北側と南側の2クラスタ、2021年は南部に集中、2025年は複数のクラスタが同時に濃くなる）。
- 原記録ベースと重複集約ベースで、ホットスポットの**位置**自体は概ね一致するが、**濃淡（密度）**は重複集約で全体的に薄まる。これは特定メッシュへの複数回通報が同一個体由来である可能性が高いことを示唆する。
- 座標は1kmメッシュ相当に丸めた上で集計しており、成果物に個別の詳細座標・番地は一切出力していない。

---

## 7. データ品質診断

**図**: `p3_quality_rates_by_year.png`（座標欠損率／時刻欠損率／重複率／平均グループサイズの年別推移）、`p3_quality_category_composition.png`（種別構成比の年変化）、`p3_quality_coordinate_precision.png`（座標小数桁数の年別分布）

### 年別の欠損率

- 座標欠損率：全期間を通じてほぼ0%（2025年にわずか0.3%程度の欠損が生じている以外は皆無）。
- 時刻欠損率：前述の通り2020年に27.4%と突出。次いで2021年が約13%。他の年は3〜7%程度。

### 種別構成比の年変化

- 目撃の構成比は年によって45〜75%程度で変動する。2025年は目撃・目撃(未確定種)・痕跡・カメラ確認のいずれの絶対件数も増加しているが、構成比で見ると痕跡とカメラ確認の割合がやや高まっている（単純な目撃通報の増加だけでなく、現地確認作業の増加も伴っている可能性を示唆する）。

### 重複率・グループサイズの年変化

- 重複率、平均グループサイズともに2019年・2025年で高く、2021年・2024年で低い。**単調な増加・減少トレンドではなく、特定年に集中する変動**である。

### 座標精度（小数桁数）の年変化

- 緯度の小数桁数の分布を年別に見ると、桁数の分布パターンに年ごとの違いが見られる（詳細は図参照）。ごく初期の記録に手入力由来と見られる少ない桁数の記録が混在する一方、GPS由来と見られる桁数の多い記録も年を通じて存在しており、**単一の記録方式に統一されているとは限らない**ことが示唆される。ただし本記述統計だけで「いつ方式が変わったか」を確定させることはできない。

### 記録方式変更を疑わせる断層（総合所見）

以下の2点が、単純な年次変動を超えた「断層」の候補として挙がる：

1. **2020年の時刻欠損率の急増（27.4%、他年の3〜4倍）**：同年は総件数も95件と少ない年であり、記録の運用（入力項目の徹底度、担当者の変更等）に何らかの変化があった可能性がある。
2. **2025年の全指標の同時変化**：総件数の急増、重複率の急増、種別構成比の変化が同時に生じている。これは単に「ヒグマの出没が急増した」という解釈と、「通報・記録の運用が変わった、あるいは同一個体が例年より広く動いて頻繁に発見された」という解釈のいずれとも整合しうる。本記述統計だけではこの2つを分離できない。

いずれも本記述統計は「断層の候補を指摘する」にとどまり、原因の特定（自治体への確認）は行っていない。

---

## 8. 原記録ベースと重複候補集約ベースの差分要約

| 観点 | 差が生じるか | 内容 |
|---|---|---|
| 季節パターンの形（どの週・月が多いか） | **小さい** | ピークの位置はおおむね一致。特に2025年秋のピーク週は両ベースで同じ。 |
| 季節パターンの高さ（ピークの大きさ） | **大きい** | 2025年秋のピークは原記録で最大36件/週、重複集約で最大16件/週と半分以下に縮小。 |
| 曜日パターンの形 | **小さい** | どの曜日が多い・少ないかの相対的な順序はおおむね類似。 |
| 曜日パターンの絶対水準 | 中程度 | 重複集約では絶対件数が全体的に約25〜35%程度減少する（グループ化による）。 |
| 時刻分布の形 | **小さい** | 目撃・痕跡それぞれの時刻分布の山の位置は両ベースで類似。 |
| 空間分布（ホットスポットの位置） | **小さい** | 位置は類似。 |
| 空間分布（密度の大きさ） | 中程度〜大きい | 重複が集中するメッシュほど、重複集約後の縮小幅が大きい。 |
| 年次総件数・重複率 | **大きい** | 2019年・2025年で重複率が特に高く、年による総件数の伸びの解釈（実際の出没増加 vs 同一個体の複数回通報）が変わりうる。 |

**総括：「どこで・いつ（季節・曜日・時刻）」というパターンの形は、重複除去の有無によらずおおむね頑健である。一方「どれくらいの規模か」という水準の解釈は、重複除去によって大きく変わりうる（特に突出した年である2025年）。** これは重複候補集約ルールが試作版（GPS較正なしの素朴な閾値）であることを踏まえても、無視できない差である。

---

## 9. H4・H6の暫定判定まとめ

| 仮説 | 暫定判定 | 根拠 |
|---|---|---|
| H4（春秋の二峰性とその年変動） | **部分的支持（季節性は支持、二峰性の一律のパターンは不支持）** | 季節集中（夏〜秋に多く冬にほぼ皆無）は明確。ただし「毎年春秋に二峰がある」という一律のパターンは、年によって単峰・複数山・不明瞭が混在するため支持されない。ピークの時期・高さの年変動自体は非常に大きく、記述的に強い結果である。 |
| H6（出没記録の曜日周期） | **曜日差の存在は支持、原因の解釈は判定不能** | 曜日別件数に約14%程度の差があり、原記録・重複集約の両ベースで頑健。ただし日付フィールドの意味が未確定であるため、野外活動由来か行政処理由来かは判定できない。 |

---

## 10. H1の扱い

**H1（素朴な出没分布と人口密度・道路密度の対応）はこの段階では判定していない。** 人口メッシュ・道路密度データを取得していない（DATA_SOURCES.md参照）ため、代替変数（例えば区・行政区画単位の粗い人口規模の推測など）による近似も本レポートでは一切行っていない。空間記述（第6節）で得られたホットスポットの位置についても、「人口密度が高い場所に多い」「少ない場所に多い」といった主張は書いていない。人口・道路データを取得した後にP4で正式に検証する。

---

## 11. 再現方法

```bash
uv run python src/ingest/fetch_sapporo_bear.py       # raw取得（P2b、既に実施済み）
uv run python src/features/build_sapporo_records.py  # レコード整形（P2c、既に実施済み）
uv run python src/features/build_sapporo_panel.py     # 日次・週次パネル（P2c、既に実施済み）
uv run python src/viz/p3_figures.py                   # 本レポートの図表一式を reports/figures/p3_*.png に生成
```

生成される図表一覧（18ファイル、すべて `reports/figures/` 配下）：

`p3_annual_counts_by_type.png`, `p3_annual_total_and_duprate.png`,
`p3_seasonality_weekly_overlay.png`, `p3_seasonality_weekly_by_type.png`,
`p3_weekday_counts.png`, `p3_weekday_by_year_heatmap.png`, `p3_weekday_by_type.png`, `p3_weekday_month_heatmap.png`, `p3_weekday_holiday.png`,
`p3_timeofday_distribution.png`, `p3_timeofday_by_month_heatmap.png`, `p3_timeofday_missing_rate.png`,
`p3_spatial_mesh_by_type.png`, `p3_spatial_mesh_yearly_raw_record.png`, `p3_spatial_mesh_yearly_dedup_group.png`,
`p3_quality_rates_by_year.png`, `p3_quality_category_composition.png`, `p3_quality_coordinate_precision.png`
