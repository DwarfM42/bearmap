[project]
name = "bearmap"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.5",
    "pandas>=3.0.5",
    "polars>=1.43.1",
    "pyarrow>=25.0.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
