# PROJECT_SPEC v2: 北海道ヒグマ出没情報の時空間分析

## 0. このドキュメントの使い方

Claude Code への作業指示書。フェーズ制で、**P0 と P1 はゲート**。
ゲートを通過できなかった場合は次に進まず、判定理由を書いて人間に返す。
「とりあえず動くものを作って達成感を出す」は不要。ここは判定を間違えると以降が全部無駄になる領域。

---

## 1. リサーチクエスチョン

**公開されているヒグマ出没情報から、ヒグマの相対的な出没・空間利用パターンと、人間側の観測・通報努力を、どこまで分離できるか。分離した上で、秋季食物資源の豊凶・気象と対応づけられるか。**

### 前提として共有しておく観測モデル

公開された出没記録1件は、次の積として生成される：

```
記録される確率 = P(ヒグマがそこにいる)
               × P(人間の生活圏側へ出る)
               × P(人間に発見される)
               × P(通報される)
               × P(自治体が記録・公開する)
```

このデータから取り出せるのは**ヒグマの実際の分布そのものではない**。
取り出せるのは、観測努力の差を一定程度補正した後の**相対的な出没・空間利用強度**にすぎない。
最終レポートを含め、この区別を崩した表現を書かないこと。

### 検証仮説

| ID | 仮説 | 位置づけ |
|----|------|----------|
| H1 | 素朴な出没件数・出没有無の空間分布は、人口密度・道路密度など人間活動の代理変数と強く対応する | 前提確認 |
| H2 | 人間活動の効果を統計的に考慮した後も、説明されない空間集積が残る（河畔林、耕作地の縁、都市近郊林の連続性など） | 本命1 |
| H3 | 秋季食物資源（堅果：ミズナラ等／液果：ヤマブドウ・サルナシ等）の不作年に、秋の出没が増える | 本命2 |
| H4 | 年内に春と秋の二峰性があり、年によって各ピークの時期・高さが変動する | 記述的だが図として強い |
| H5 | 融雪の早い年は春のピークが前倒しになる | サブ |
| **H6** | **出没記録に曜日周期が存在する** | **観測過程の直接測定。下記参照** |

#### H1 の評価について

「決定係数が高い」といった指標を先に固定しない。
出没件数はゼロ過剰・過分散・空間依存を持つため、通常の線形回帰の R² は不適切。
評価指標は P3/P4 でモデル形に応じて選ぶ（疑似決定係数、deviance explained、AIC、交差検証予測性能、人間活動変数の投入前後での改善量など）。
**OLS を既定の道具として使わないこと。**

#### H4 の扱い

検証するのは「二峰性があり、ピークの時期・高さが年変動する」というパターンまで。
「春＝若齢雄の分散」「秋＝過食期」は**解釈候補**であり、公開出没情報には性・年齢情報が通常含まれないため直接検証できない。
`FINDINGS.md` では検証結果と解釈候補を明確に節を分けて書く。

#### H6 について（本プロジェクトの識別戦略の核）

ヒグマに週周期はない。人間にはある。
したがって出没記録の曜日変動は、ほぼ純粋に**観測・通報過程の信号**である。その振幅は観測努力効果の下限を与える。

ただし日付フィールドの意味で解釈が反転する：
- 記録が**発生日**ベース → 週末にピーク（人間の野外活動）
- 記録が**受理日・公表日**ベース → 平日にピーク（行政の稼働）

どちらであるかを P1 で必ず確定させる。取り違えると解釈が逆になる。
時刻が記録されている場合は、ヒグマの薄明薄暮性と人間の日中活動の畳み込みとして同型の議論ができる。副次的に検討する。

---

## 2. フェーズ

### P0. 先行研究サーベイ 【ゲート】

**コードは書かない。**

#### 検索軸

1. 北海道のヒグマ出没データを使った時空間統計解析（道総研、大学の野生動物管理系研究室）
2. 出没情報の**報告・観測バイアス補正**を明示的に扱った研究
3. クマ類の出没と秋季食物資源の豊凶の関係（本州ツキノワグマには蓄積がある前提。**北海道ヒグマでどこまでやられているか**が焦点）
4. 上記2と3を**同一の分析枠組みで**扱っている研究 ← 本命の隙間
5. 手法一般（地域を問わない）：
   - `citizen science reporting bias`
   - `opportunistic wildlife sightings`
   - `point process sampling bias` / `target-group background`
   - `integrated species distribution model` / `integrated distribution model`
   - `ecological count data observation process`
   - `detection probability` / `zero-inflated` / `hurdle model`

検索は日本語・英語の両方。J-STAGE、CiNii、Google Scholar、道総研の刊行物一覧、環境省・林野庁の報告書。
`brown bear`, `Ursus arctos`, `Hokkaido`, `human-bear conflict`, `mast seeding`, `sighting reports` の組み合わせも回す。

#### データ型と手法の対応（サーベイ時の観点）

実際の自治体データがどの形かで、参照すべき手法系統が変わる。両方を意識して探す。

| データ形式 | 手法系統 |
|---|---|
| 個票の点座標がある | point process / presence-only / MaxEnt 系、target-group background |
| メッシュ別の件数 | 負の二項、zero-inflated、hurdle、空間ランダム効果付き GLM/GAM |
| 市町村別の件数 | パネル回帰、階層ベイズモデル |

#### 「既にやられている」の判定基準

**既にやられている**（すべて満たす）
- 北海道内のヒグマを対象
- 複数年データ
- 出没または人間との軋轢を目的変数としている
- 該当する説明変数（H2ならバイアス補正、H3なら食物資源豊凶）を明示的に扱っている
- 統計的または定量的に検証している

**部分的**（いずれか）
- 北海道・ヒグマだが定性的な記述にとどまる
- クマ類だが北海道外（本州ツキノワグマ等）
- 北海道ヒグマだが単年
- 豊凶ではなく食性・胃内容物分析のみ
- 出没ではなく捕獲数のみを扱っている

**見当たらない**：上記いずれにも該当する文献が確認できない

#### NO-GO 条件

以下を**すべて**満たす研究が存在する場合のみ NO-GO：

- 北海道内のヒグマ出没データを対象としている
- 観測・報告バイアスを**明示的に**補正している
- 補正後の空間パターンと、秋季食物資源の豊凶または気象との関係を、**同一の分析枠組み（または明確に連続した一連の研究）で**検証している
- 複数年・空間単位が明示されている
- 査読論文であるか、方法が詳細に記載された報告書である

**H2 相当の研究と H3 相当の研究が別々に存在するだけでは NO-GO にしない。** 統合する価値が残る。その場合は CONDITIONAL とし、統合の設計案を提示する。

#### P0 調査品質ルール

- タイトル、検索結果スニペット、二次引用だけで研究内容を断定しない。
- 全文または要旨で方法を確認できない文献は「内容未確認」と明記し、GO/NO-GO 判定の主要根拠にしない。
- 査読論文、学位論文、行政・研究機関報告、学会要旨、一般記事を区別する。
- 同じデータや研究を用いた重複発表は1系列として扱う。
- 仮説を支持する研究だけでなく、**不支持・無関係・地域差が大きい・データ品質上検証不能と結論した研究も優先的に探す**。各仮説について最低1回は否定的・無関連の結果を意図して検索する。
- 「北海道」「ヒグマ」「出没または軋轢」「観測・報告バイアス補正」「食物資源豊凶」の各条件を、文献ごとに明示的にチェックする。
- URL は実際に開いて確認したものだけ記載する。DOI があれば記録する。
- ペイウォール等で全文を確認できなかった場合は、その制約を記録する。
- **関連性の高い資料を最低15件、最大40件確認した時点で判定する。** ただし北海道ヒグマに直接関係する文献は上限に達しても優先して確認する。

#### 成果物: `SURVEY.md`

文献ごとに以下の列を持つ表：

| 列 | 内容 |
|---|---|
| 著者・年・タイトル | |
| URL / DOI | 実際に開いたもののみ |
| 資料種別 | 査読論文 / 学位論文 / 機関報告 / 学会要旨 / 一般記事 |
| 査読 | 有 / 無 / 不明 |
| 対象地域 | |
| 対象期間 | 年数 |
| 対象種 | |
| データ種別 | 目撃通報 / 捕獲 / テレメトリ / カメラ / その他 |
| バイアス補正手法 | 手法名、または「なし」 |
| 関連仮説 | H1–H6 |
| 確認レベル | **全文確認 / 要旨のみ / 内容未確認** |
| 要約 | 2行 |

加えて：
- 各仮説について「既にやられている／部分的／見当たらない」の判定と、その根拠となる文献ID
- **GO / CONDITIONAL / NO-GO の判定と理由**
- CONDITIONAL の場合：新規性が立つ方向の案を2つ以上

判定を人間に返して**停止**。承認なしに P1 へ進まない。

---

### P1. データ取得可能性の確認 【ゲート】

**取得の可否と粒度だけ確認する。本格的な整形はしない。**
URL やデータセット名を**推測で書かない**。実際にアクセスして確認できたものだけ記録する。

#### A. 出没情報（これが全ての律速）

- 北海道のヒグマ出没情報（北海道オープンデータポータル、環境生活部の公表資料）
- 札幌市のヒグマ出没情報（道内でも粒度が細かい可能性が高い）

**確認必須項目：**

1. **位置の粒度**：点座標 / 町丁目 / メッシュ / 市町村 のどれか
2. **時間の粒度**：日 / 週 / 月
3. **日付フィールドの意味**：**発生日か、通報受理日か、公表日か** ← H6 の解釈が反転する。複数フィールドがあれば全て取得
4. **時刻が記録されているか**
5. **種別が区別されているか**：目撃 / 痕跡（足跡・糞） / 農作物被害 / 人身事故。種別ごとに検出確率が桁で違うため、区別できないと H2・識別戦略が大きく弱まる
6. **カバー期間**：年数。10年未満だと豊凶との対応は苦しい
7. **記録方式の変更履歴**：途中で様式・公開範囲・受付体制が変わっていないか。変わっていれば時系列に断層が入る
8. 重複記録（同一事案の複数通報）の扱い

#### B. 人間活動・観測努力の変数

- 人口：国勢調査メッシュ人口（e-Stat）
- 道路：OpenStreetMap（種別ごとの延長をメッシュ集計）
- 建物・宅地：国土数値情報 土地利用細分メッシュ
- **真の観測努力に近い変数**（取得できれば価値が非常に高い）：パトロール回数・実施日、カメラトラップ稼働日数、登山届の受理件数、自然公園入園者数、自治体の通報受付体制
  - **入手できない可能性が高い。無理なら「取得不可」と明記して外す。推測で埋めない**

#### C. 環境・植生

国土数値情報（土地利用、河川、標高）、環境省 自然環境保全基礎調査 植生図、国土地理院 DEM

#### D. 秋季食物資源の豊凶（H3 の核）

- 道総研 林業試験場、林野庁北海道森林管理局、市町村の豊凶調査
- 堅果（ミズナラ等）と液果（ヤマブドウ・サルナシ等）は**別の資源として区別して記録する**
- 年 × 地域（振興局単位程度）で数値化されたもの。「豊作／並／不作」の3段階でも可
- 取れない場合の代替：開花期の気象条件からの推定（精度は落ちる。**代替であることを明記して使う**）

#### E. 気象

気象庁 過去の気象データ（AMeDAS 日別）。気温、降水、積雪深（→ 融雪日）、前年夏の気温。

#### 成果物: `DATA_SOURCES.md`

上記すべてを表形式で：データ名 / 提供元 / URL / 粒度 / 期間 / 形式 / ライセンス / 取得日 / 取得可否 / 備考

**停止条件**：出没データの位置粒度が市町村単位しかない場合 → メッシュ解析が成立しない。その事実を報告して停止し、市町村パネルでの縮小版設計を代案として提示する。

---

### P2. データ取得・パネル構築

- 生データは `data/raw/` に**取得したまま保存**（再取得できなくなる場合がある）
- 空間単位は P1 の結果で決める。第一候補 1km メッシュ、点座標なら 500m も検討
- 時間単位は週。ただし**曜日効果の検証（H6）のため、日単位の系列も別途保持する**
- 最終形：`空間単位 × 週` のパネル。列は出没件数（**種別ごとに分けて保持**）、人間活動変数、環境変数、気象、豊凶
- **ゼロの扱い**：集計上のゼロは「公開記録された出没が0件」であることを意味し、**ヒグマの不在を意味しない**。未観測ゼロが混在している。欠損とは区別して保持し、削除も補間もしない。本プロジェクトの主題そのものがこのゼロの背後にある観測過程なので、真のゼロとして扱わない
- 保存は parquet、集計は DuckDB

### P3. 記述統計（補正なし）

意図的に「素朴な図」を先に作る。最終レポートにも残す。

- 出没密度のヒートマップ
- 人口密度・道路密度との関係 → **H1**
- 週別の年内分布、年ごとに重ね描き → **H4**
- **曜日別の件数分布 → H6**。日付フィールドの意味と併せて解釈する
- 上記すべてを**種別ごとに分けて**作成する

### P4. 観測過程を考慮したモデル

#### 変数の扱い（重要）

- **人口・道路密度・建物密度をオフセットに入れない。** これらは観測努力の代理であると同時に、ヒグマが人間環境へ接近する生態的要因でもある。オフセットにすると「人口が倍なら件数も倍」という係数1の仮定が入る。原則として**係数を推定する説明変数**として投入する
- オフセットが許されるのは、**真の観測努力量に近い変数が取得できた場合のみ**（パトロール回数、カメラ稼働日数、観測時間、通報受付対象人口×期間など）

#### 識別可能性について（明記すること）

人口を説明変数に降格させても、その推定係数は「発見・通報されやすさ」と「ヒグマが人里へ接近する度合い」の**混合**である。
これはモデルの改善であって、識別問題の解決ではない。
**「係数を推定したので補正済み」と書かないこと。** `FINDINGS.md` に、何が分離できて何ができていないかを明示的に書く。

分離を助ける材料は以下の3つしかない。使えるものを使い、使えないものは使えないと書く：

1. **曜日効果（H6）**：ヒグマに週周期がないため、ほぼ純粋な観測過程の信号。努力効果の下限を与える
2. **種別間の検出確率差**：人身事故は報告率がほぼ100%、目撃ははるかに低い。同一空間・同一期間で「事故はあるが目撃報告が少ない」領域は通報努力が低い領域である可能性が高い。種別を独立に解析するのではなく、**検出確率の異なる複数の観測窓**として扱う
3. **準・独立データとの突合**：捕獲・駆除記録など（ただしこれ自体も対応努力のバイアスを持つ。無バイアスの参照系ではない）

#### モデル

- ベースライン：負の二項回帰（過分散が確実にあるためポアソンは使わない）。ゼロ過剰の程度に応じて zero-inflated / hurdle も比較
- 空間相関：空間ランダム効果（CAR/ICAR 等）ありなしを比較
- 非線形性：標高・道路距離は単調でない可能性が高いので GAM 版も作る
- P1 で点座標が得られた場合は、point process / target-group background 系も検討する
- **H2 の判定**：人間活動変数で説明した後の残差に空間構造が残るか。Moran's I とその地理的位置

### P5. 食物資源・気象との突合

- **n の確認を先に行う。** 年数が10年なら独立標本は実質10。多変量を投げ込む余地はない
- n が小さい場合は、モデリングではなく記述的な提示に留め、**その旨を明記する**
- 堅果と液果を分けて投入 → **H3**
- 融雪日と春ピーク日の対応 → **H5**

### P6. レポート

**成果物: `FINDINGS.md`**

- 仮説ごとに「支持／不支持／判定不能」と根拠図表。**判定不能を判定不能と書く**
- 検証結果と解釈候補を節で分ける
- 限界セクション（必須）：識別できていない交絡、観測努力の代理変数の限界、豊凶データの空間解像度、因果ではなく関連であること、記録方式変更の断層
- 再現手順

---

## 3. 技術スタック

Python 3.11+ / uv または venv、polars・pandas、geopandas、duckdb、statsmodels、pygam、matplotlib、folium。
空間ベイズが必要になったら pymc。**ただし P4 のベースラインが動くまで導入しない。**
notebook は探索用のみ。最終図表は `src/` のスクリプトから再生成できること。

## 4. ディレクトリ構成

```
higuma/
├── README.md
├── PROJECT_SPEC.md
├── SURVEY.md                # P0 成果物
├── DATA_SOURCES.md          # P1 成果物
├── FINDINGS.md              # P6 成果物
├── data/{raw,interim,processed}/    # raw は gitignore
├── src/{ingest,features,models,viz}/
├── reports/figures/
└── tests/
```

## 5. やってはいけないこと

- **データ・URL・文献の捏造。** 見つからないものは「見つからない」と書く。それが最も価値のある出力になる場合がある
- スニペットだけで先行研究の内容を断定する
- 欠損を静かに補間する（埋めるなら方法を `DATA_SOURCES.md` に記録）
- 記録上のゼロを「ヒグマ不在」として扱う
- 人口・道路密度をオフセットに固定する
- 「補正済み」「実際の分布」といった、識別できていないものを識別できたかのように書く表現
- 相関を因果として書く
- **出没地点の詳細座標を成果物に出す**（私有地・個人宅が特定される。公開図は最小でも1kmメッシュに丸める）
- ゲート（P0/P1）を人間の承認なしに通過する
- 有意にならなかった変数を静かに落として残った結果だけ報告する

## 6. 最初の指示

**P0 のみ実行してください。** `SURVEY.md` を作成し、GO/CONDITIONAL/NO-GO の判定と理由を提示して停止すること。
