# P4_MODEL_PLAN.md — 分析仕様の事前固定

作成日：2026-07-27（P4a完了時点）。**この文書に記載した仕様は、P4bでモデルを実行した後の結果を見て
変更しない。** 変更が必要になった場合は、本文書末尾の「変更履歴」に変更内容・理由・変更日を追記し、
元の仕様も削除せず残す。

対象範囲：札幌市データのみ（全道は対象外。P2_GATE.md参照）。

---

## 0. 全体設計：2トラック構成

出没データは1,568件（9年間）と少なく、1kmメッシュ×週単位で交差させると大半のセルが恒常的にゼロになる
（P4a-2で1kmメッシュの非ゼロセル数292、非ゼロセルの中央値ですら3件/9年）。これを無理に単一の
「メッシュ×週」パネルとして空間・時間の両方を同時に解像しようとすると、事実上ほぼ全てのセルが
構造的ゼロとなり、空間ランダム効果と季節項の両方を安定して推定することは困難と判断した。

そのため、以下の2トラックに分けて別々のモデルとして推定し、結果を比較する
（PROJECT_SPEC.mdの「単一の統合モデルに強引に落とし込まない」という設計方針、および
P2_GATE.mdでの札幌市/全道の切り分けと同じ考え方を、時間・空間の解像度の切り分けにも適用する）。

- **トラックA（時間トラック）**：空間集計なし（札幌市全体を1単位として扱う）。週単位。
  季節性(H4)・曜日周期(H6)の正式検証を担う。P3の記述統計を正式なモデルへ格上げする位置づけ。
- **トラックB（空間トラック）**：1kmメッシュ×年（または季節区分×年）単位。人口・道路等の
  共変量投入後の残差空間構造(H2)、および人口・道路密度との対応(H1、判定基準は限定的)を担う。

両トラックとも、**原記録ベース・重複候補集約ベースの両方を並行して実行する**（どちらか一方を
「正しい」データとして選ばない）。P4a-4で作成した閾値感度分析（厳しい／基準／緩い）は、
基準（v1）モデルの結果に対する頑健性チェックとして併記する（主結果としては扱わない）。

---

## 1. 目的変数

### トラックA
- 週間件数（`event_type_normalized`別）。主目的変数は`sighting`（目撃、n最大）。
  `track_sign`（痕跡）・`camera`（カメラ確認）は副次的目的変数として同一モデル構造で並行して推定し、
  種別間で係数（特に曜日効果・季節効果）が異なるかを比較する（検出確率の異なる観測窓としての位置づけ）。
- 単位はP2c/P3で作成済みの`sapporo_panel_weekly.parquet`と同じ定義（ゼロ埋め込み済み、
  データ収録期間内のみ）。

### トラックB
- 1kmメッシュ×年間件数（全種別合計、および`sighting`単独の2系列）。
- 季節区分（春:3-5月、夏:6-8月、秋:9-11月、冬:12-2月）×年×メッシュも副次的に作成し、
  年単位モデルとの結果比較に用いる。ただし季節×メッシュ×年は更に疎になるため、
  主モデルは「メッシュ×年」とし、季節区分版は頑健性チェック（H3が将来接続された際の
  布石）として位置づける。

---

## 2. 空間・時間単位

- 空間単位：**1kmメッシュ（JIS X0410 標準地域メッシュ 3次メッシュ）**。P4a-2で500m/1km/2kmを比較し、
  1kmを採用（非ゼロセル数292、中央値3件/9年、2kmほど粗すぎず500mほど疎すぎない中間点、
  かつ人口・土地利用メッシュ等の公式統計との整合性が高いため）。
- 時間単位：トラックAは週（ISO週）。トラックBは年（暦年、ヒグマの活動期3-12月を通年として扱う。
  月区分の定義は上記の通り）。

---

## 3. 主説明変数

### トラックA（時間トラック）
- 年（カテゴリ変数またはランダム効果。2025年の扱いは第5節参照）
- ISO週番号の季節平滑化項（GAMのcyclic smooth term、または月ダミー）
- 曜日（カテゴリ変数、月～日の6自由度）
- 種別（`sighting`/`track_sign`/`camera`をモデル間で separate に推定。同一モデル内で交互作用としては
  扱わない）

### トラックB（空間トラック）
- 人口（2020年国勢調査、非補間・全年同一値。人口をオフセットには入れず、係数を推定する説明変数として
  投入する。PROJECT_SPEC.mdの指示通り、人口が2倍なら件数も2倍という係数1の仮定を置かない）
- 道路：主要道路（motorway/trunk/primary/secondary/tertiary/residential/unclassified）の
  メッシュ内総延長（m）、およびメッシュ中心から最も近い主要道路までの距離（m）。両方投入し、
  どちらが説明力を持つかをモデル比較で確認する（同時投入時の多重共線性は事前にVIFで確認する）。
- 土地利用：宅地系（residential/commercial/industrial/retail等）被覆率、森林被覆率
  （建物フットプリントは公開Overpassインスタンスの応答が重すぎて取得断念したため、
  宅地被覆率を建物密度の代理変数として使う。この代替はDATA_SOURCES.md/P4a側に明記済み）
- 森林縁：森林/非森林境界（森林ポリゴンの外側）までの距離
- 河川までの距離
- 標高、傾斜（DEMから算出）

**変数選択は結果を見て行わない。** 上記すべてを主モデルに投入し、有意にならなかった変数を
黙って落とすことはしない（PROJECT_SPEC.mdの禁止事項）。多重共線性がVIF>10等で問題になる場合のみ、
統計的な理由を明記した上で変数を統合・除外する（この場合も変更履歴に残す）。

---

## 4. 交互作用

事前に投入する交互作用は最小限とし、探索的な追加は行わない。

- トラックA：**季節区分（春/夏/秋/冬）× 種別**。種別ごとに季節パターンの形が異なるか
  （P3で目撃と痕跡の季節ピークの位置がやや異なる可能性を示唆したため）を確認する、唯一の事前登録済み交互作用。
- トラックB：**森林被覆率 × 森林縁までの距離**。「森林に近い開けた土地」（森林縁効果）を
  捉えるための交互作用。これ以外の交互作用（人口×道路等）は事前登録しない。

---

## 5. 2025年の扱い

2025年は原記録数361件（重複集約後208件）と、他年（平均156件、重複集約後平均123件）を大きく上回る
突出した年である（P4a-3参照）。北海道新聞の報道（2025年、木の実の壊滅的凶作と関連づけ）と
時期的に整合するが、これは時間的な符合であり本プロジェクトで豊凶データを突合した結果ではない。

**方針：2025年を通常年として主分析に含める。ただし2025年を除外した感度分析を必ず併記する。**

理由：
- 2025年を「特別レジーム」として除外すると、本プロジェクトが検出したいかもしれない
  「食物資源不足時の出没増加」という信号そのものを除外してしまう可能性がある。
- 逆に2025年の重複率が突出して高い（42.4%、閾値感度分析でも一貫して他年より高い傾向、
  P4a-4参照）ため、原記録ベースの結果が2025年に強く引っ張られる可能性は無視できない。

これに対応するため、トラックA・トラックBとも以下の2系列を必ず両方報告する：
1. 全期間（2017-2025年）を用いた主分析
2. 2025年を除外した感度分析（2017-2024年）

両者で結論（有意性、係数の符号）が変わる場合、その差自体を結果として明記する
（「2025年に牽引された結果である」という限界として記載する）。

---

## 6. 原記録／重複集約／閾値感度分析の扱い

- **主分析は原記録ベースと重複候補集約ベース（試作ルールv1）の両方を並行して実行し、両方を主結果として報告する。** どちらか一方を「正しい」データとして選ばない（P3の方針を継承）。
- P4a-4で作成した閾値感度分析（厳しい/基準/緩いの3条件）は、基準（v1）モデルの結果に対する
  頑健性チェックとして報告する。具体的には、基準モデルで有意だった効果（曜日効果、森林縁効果等）が、
  厳しい条件・緩い条件でも同じ符号・同程度の水準で維持されるかを確認する。
- 重複判定ルールはいずれも「試作」であることを最終報告（FINDINGS.md相当）でも明記し、
  「正しく重複除去した結果」という表現は使わない。

---

## 7. 欠損処理

- `date_semantics`（日付フィールドの意味）が未確定であるため、日付・曜日を用いた分析結果は
  すべて「曜日差の存在」のみを報告し、発生日ベースか受理日ベースかに依存する解釈
  （野外活動由来か行政処理由来か）はいずれも行わない。日付フィールドの意味確認が完了した場合のみ、
  本節を更新し解釈を追加する（変更履歴に記録）。
- 時刻（`event_time_raw`から解析した`hour`）の欠損は年・種別で偏りがある（P4a-3で確認：
  2020年の痕跡確認で72.2%欠損、生値は"不明"という明示的な值）。時刻はトラックA・Bのいずれの
  主モデルにも説明変数として使わない（欠損が非ランダムであるため、時刻を使った分析は本モデルの
  範囲外とし、別途「時刻分布」という記述統計としてのみP3で扱う）。
- 座標欠損（`location_precision`=`missing`、1件のみ）はトラックB（空間トラック）からは除外する。
  トラックA（空間集計なし）には引き続き含める。
- 人口共変量は2020年国勢調査の単年値を2017-2025年の全年に同一に割り当てる（非補間版）。
  この仮定はモデル全体に適用され、年による人口変化（例えば人口減少地域での経年変化）は
  捉えられない。この限界を最終報告に明記する。
- 記録上のゼロ（トラックAの週次件数、トラックBのメッシュ×年件数）は「その期間・空間単位に
  記録された出没が0件」であることのみを意味し、ヒグマの不在を意味しない（PROJECT_SPEC.mdの
  大原則）。データ収録期間・空間範囲の外側には決してゼロを外挿しない。

---

## 8. モデル比較指標

PROJECT_SPEC.mdの指示通り、OLSのR²は使わない。

- **ベースライン分布**：負の二項回帰（過分散を仮定、ポアソンは使わない）。ゼロ過剰の程度に応じて
  zero-inflated / hurdle モデルも比較する。
- **モデル選択・比較**：AIC（および可能であればWAIC）。ネストしないモデル間の比較にも使えるよう、
  交差検証による予測性能（leave-one-year-out CV、対数尤度またはDeviance基準）を併用する。
- **トラックB空間ランダム効果の要否判定（H2関連）**：共変量のみのモデルと、共変量+空間ランダム効果
  （ICAR/CARまたは簡易にMoran's Iで残差の空間自己相関を検定）モデルを比較し、AIC/WAICの改善幅と
  残差のMoran's I（p値）の両方を報告する。
- **人間活動変数投入前後の改善量（H1関連）**：人口・道路変数を含まないnullモデルと含むフルモデルの
  deviance explained（1 - residual deviance/null deviance）の差を報告する。

---

## 9. H1・H2・H6の判定基準

### H1（素朴な出没分布と人間活動代理変数の対応）
**この段階では正式判定を行わない**（人口・道路データが取得できたため、P4bで初めて検証可能になる）。
判定基準（P4b実施後に適用）：人口・道路密度いずれかの係数が95%信頼区間で0を含まず、
かつ符号が「人間活動が多いほど出没記録が多い」という向きであれば「支持」。
ただし係数が有意であっても、**これは「発見・通報されやすさ」と「ヒグマが人里へ接近する度合い」の
混合であり、どちらか一方に帰属させる表現（「人口効果を補正した」「実際の分布」等）は使わない**
（SURVEY.md・PROJECT_SPEC.mdの識別可能性に関する原則を継承）。

### H2（人間活動補正後の残差空間集積）
共変量（人口・道路・土地利用・地形・河川・森林縁）をすべて投入した後の残差に、統計的に有意な
空間自己相関（Moran's I、p<0.05）が残るか、または空間ランダム効果を含むモデルがAIC/WAICで
明確に改善するかで判定する。有意な残差空間構造が確認されれば「支持」、確認されなければ
「不支持（少なくとも投入した共変量の範囲では説明済み）」、閾値感度分析やトラック間で結論が
割れる場合は「判定不能」とする。

### H6（出没記録の曜日周期）
トラックAモデルで曜日固定効果が尤度比検定で有意（p<0.05）であれば「曜日差の存在を支持」。
ただし**原因（野外活動由来か行政処理由来か）については、date_semanticsが確定しない限り
一切の解釈を行わない**。曜日効果の大きさ（係数の推定値）は報告するが、それを「観測努力の代理」
として他の変数の補正に使う、という用途には現時点では使わない。

---

## 10. 変更履歴

（P4b実施前の時点では変更なし。変更が生じた場合はここに追記する）

| 日付 | 変更内容 | 理由 | 変更前の記載 |
|---|---|---|---|
| 2026-07-28 | NB回帰の推定方法を、statsmodels `NegativeBinomial`（MLEで係数とalphaを同時推定）から、Cameron & Trivedi (1990) の二段階推定（Poisson IRLS→補助回帰でalpha推定→alpha固定でNB IRLS）に変更 | 月(12)×年(9)等の多数の固定効果を同時投入すると、MLE同時推定が特異行列・非収束・非有限AICを頻発した（特に件数の少ない種別）。分布族（負の二項）・比較指標（AIC・疑似決定係数）自体は変更していない | 「負の二項回帰（NB2、statsmodels）」とのみ記載、具体的な推定アルゴリズムは未指定 |
| 2026-07-28 | トラックB「非線形性の必要性を評価」した結果、距離・標高・傾斜の連続変数に二次項を追加した拡張モデルを採用（第8節の「診断上必要と判断された拡張モデルのみ追加」という原則の適用結果） | 線形モデルに対し二次項付きモデルがAICで大きく改善（尤度比検定p<0.001）し、非線形性が統計的に支持されたため | 交互作用は「森林被覆率×森林縁距離」のみを事前登録、二次項は未記載 |
| 2026-07-28 | トラックB残差の空間自己相関（H2診断）が有意だったが、CAR/ICAR空間ランダム効果モデルの本格実装は行わず、Moran's Iの結果自体をH2の証拠として採用し、CAR/ICAR実装は次フェーズの課題とした | PROJECT_SPEC.mdの「P4のベースラインが動くまでpymcを導入しない」という方針、および1kmメッシュ×年パネルの疎さ（中央値3件/9年）から階層的空間モデルの識別可能性に懸念があるため | 第8節「共変量のみのモデルと、共変量+空間ランダム効果（ICAR/CARまたは簡易にMoran's Iで残差の空間自己相関を検定）モデルを比較」という記載に対し、簡易な方（Moran's I検定）のみ実施 |
